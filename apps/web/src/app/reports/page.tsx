import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Semua Laporan" };

export default function ReportsPage() {
  return (
    <PlaceholderPage
      eyebrow="Public browse"
      title="Semua laporan LOST dan FOUND"
      description="Halaman ini sudah tersedia sebagai route App Router. Filter, pencarian, dan data Supabase akan diimplementasikan pada LNFTI-17."
    />
  );
}
